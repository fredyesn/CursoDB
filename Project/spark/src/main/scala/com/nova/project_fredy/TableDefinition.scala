package com.nova.project_fredy

import org.apache.spark.sql.SparkSession

object TableDefinition {
  def create_table(tab_name: String, tab_bbdd: String)(implicit spark: SparkSession): Unit ={

  }
}
